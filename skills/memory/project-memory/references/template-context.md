Project memory for __PROJECT__. Umbrella collection indexing every subsystem under `project-memory/` at the project root. Currently contains: lesson-learned (per-session retrospectives — mission intent, prompt evolution, steering moments, wins, frustrations, skills used, takeaways). Future subsystems land as sibling folders under `project-memory/` and are indexed by this same collection automatically. Each subsystem owns its own `_INDEX.md` curated catalog. Query this collection to recall prior work, reuse heuristics, or avoid repeating mistakes.
